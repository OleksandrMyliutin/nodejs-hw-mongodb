type: object
properties:
  status:
    type: integer
    description: HTTP-статус відповіді (наприклад, 400, 401, 404, 500)
    example: 404
  message:
    type: string
    description: Опис помилки
    example: "Contact not found"
  data:
    nullable: true
    description: null (відсутні дані у випадку помилки)
    example: null
required:
  - status
  - message
  - data
