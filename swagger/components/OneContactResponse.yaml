type: object
properties:
  status:
    type: integer
    description: HTTP-статус відповіді (наприклад, 200, 201)
    example: 201
  message:
    type: string
    description: Повідомлення про результат операції
    example: "Contact created successfully"
  data:
    $ref: "./Contact.yaml"
required:
  - status
  - message
  - data
