tags:
  - Contacts
summary: Отримати список контактів
description: Повертає список контактів з підтримкою пагінації та сортування.
operationId: getAllContacts
security:
  - bearerAuth: []
parameters:
  - in: query
    name: page
    schema:
      type: integer
      minimum: 1
    description: Номер сторінки (пагінація)
  - in: query
    name: perPage
    schema:
      type: integer
      minimum: 1
    description: Кількість контактів на сторінку
  - in: query
    name: sortBy
    schema:
      type: string
    description: Поле для сортування (наприклад, "name")
  - in: query
    name: sortOrder
    schema:
      type: string
      enum: [asc, desc]
    description: Порядок сортування 'asc' (за зростанням) або 'desc' (за спаданням)
responses:
  '200':
    description: Успішне отримання списку контактів
    content:
      application/json:
        schema:
          type: object
          properties:
            status:
              type: integer
              example: 200
            message:
              type: string
              example: Successfully found contacts!
            data:
              type: object
              properties:
                contacts:
                  type: array
                  items:
                    $ref: '../../components/Contact.yaml'
                page:
                  type: integer
                  example: 1
                perPage:
                  type: integer
                  example: 2
                totalItems:
                  type: integer
                  example: 1
                totalPages:
                  type: integer
                  example: 1
                hasPreviousPage:
                  type: boolean
                  example: false
                hasNextPage:
                  type: boolean
                  example: false
        examples:
          Success:
            summary: Список контактів з пагінацією
            value:
              status: 200
              message: "Successfully found contacts!"
              data:
                contacts:
                  - _id: "687113f0d29c1342da1cb9f1"
                    name: "Hanna"
                    phoneNumber: "+380000000008"
                    contactType: "personal"
                    userId: "6870fa1264fcd7118fa2d80c"
                    photo: "https://res.cloudinary.com/dica60k30/image/upload/v1752241198/contacts/a0drczlmkphchls0thcc.png"
                    __v: 0
                page: 1
                perPage: 2
                totalItems: 1
                totalPages: 1
                hasPreviousPage: false
                hasNextPage: false
  '400':
    description: Некоректний запит
    content:
      application/json:
        schema:
          $ref: '../../components/ErrorResponse.yaml'
        examples:
          BadRequest:
            summary: Некоректний запит
            value:
              status: 400
              message: "Некоректний параметр пагінації"
              data: null
  '401':
    description: Неавторизовано
    content:
      application/json:
        schema:
          $ref: '../../components/ErrorResponse.yaml'
        examples:
          Unauthorized:
            summary: Неавторизовано
            value:
              status: 401
              message: "Authorization header missing or malformed"
              data: null
