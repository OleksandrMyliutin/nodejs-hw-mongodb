tags:
  - Contacts
summary: Створити новий контакт
description: Додає новий контакт для користувача. Дані передаються у форматі multipart/form-data.
operationId: addContact
security:
  - bearerAuth: []
requestBody:
  required: true
  content:
    multipart/form-data:
      schema:
        type: object
        required:
          - name
          - phoneNumber
          - contactType
        properties:
          name:
            type: string
            description: Ім'я контакта
            example: Test Contact
          email:
            type: string
            description: Електронна пошта
            example: testcontact@example.com
          phoneNumber:
            type: string
            description: Номер телефону
            example: "1234567890"
          contactType:
            type: string
            description: Тип контакта (наприклад, "work", "personal")
            example: work
          isFavourite:
            type: boolean
            description: Чи обраний контакт улюбленим (опційно)
            example: false
          photo:
            type: string
            format: binary
            description: Фото контакта (опційно)
responses:
  '201':
    description: Контакт створено
    content:
      application/json:
        schema:
          $ref: '../../components/Contact.yaml'
  '400':
    description: Некоректний запит
    content:
      application/json:
        schema:
          $ref: '../../components/ErrorResponse.yaml'
  '401':
    description: Неавторизовано
    content:
      application/json:
        schema:
          $ref: '../../components/ErrorResponse.yaml'